name: Test basic clean transcript (no violations)
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create clean transcript fixture
        run: |
          cat > /tmp/clean-transcript.jsonl <<'EOF'
          {"tool":"Read","tool_input":{"file_path":"README.md"},"session_id":"sess-001"}
          {"tool":"Grep","tool_input":{"pattern":"foo"},"session_id":"sess-001"}
          {"tool":"Glob","tool_input":{"pattern":"**/*.js"},"session_id":"sess-001"}
          {"tool":"Write","tool_input":{"file_path":"output.js"},"session_id":"sess-001"}
          {"tool":"Bash","tool_input":{"command":"npm run test"},"session_id":"sess-001"}
          EOF

      - id: action
        uses: .
        with:
          transcript-path: /tmp/clean-transcript.jsonl
          strict: 'false'

      - name: Assert violations output is zero
        env:
          VIOLATIONS: ${{ steps.action.outputs.violations }}
        run: |
          echo "violations: $VIOLATIONS"
          if [ "$VIOLATIONS" != "0" ]; then
            echo "FAIL: expected 0 violations, got $VIOLATIONS"
            exit 1
          fi
          echo "PASS"

      - name: Assert score is 100
        env:
          SCORE: ${{ steps.action.outputs.score }}
        run: |
          echo "score: $SCORE"
          if [ "$SCORE" != "100" ]; then
            echo "FAIL: expected score 100, got $SCORE"
            exit 1
          fi
          echo "PASS"

      - name: Assert report is non-empty
        env:
          REPORT: ${{ steps.action.outputs.report }}
        run: |
          echo "report: $REPORT"
          if [ -z "$REPORT" ]; then
            echo "FAIL: expected non-empty report"
            exit 1
          fi
          echo "PASS"
